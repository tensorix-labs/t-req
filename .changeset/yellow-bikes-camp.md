---
"@t-req/core": patch
---

Add deterministic `.http` serialization APIs:
`serializeRequest` and `serializeDocument`, plus `SerializableRequest`/`SerializableDocument` exports.
Includes parser-compatible serializer test coverage.
